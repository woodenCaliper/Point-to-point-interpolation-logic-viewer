# Point-to-point-interpolation-logic-viewer

このリポジトリは現在、**仕様策定フェーズ**です。  
実装はまだ開始しておらず、実行可能な `index.html` は配置しません。

## 現在含まれる内容
- `human requirement.md`: 人間側の要件定義
- `AI implementation instructions..md`: 実装再開時に使うAI向け実装指示

## 方針
- 実装開始の明示指示があるまで、コードファイル（例: `index.html`）は追加しない。
- 仕様確定・更新は上記2つのドキュメントに対して行う。
- 実装再開後は GitHub Pages で公開し、ブラウザから自由に試せる形で提供する。
- GitHub Pages の公開手順は、GitHub公式ドキュメントで一般的な方法（Pages設定または公式Actions）を優先する。
